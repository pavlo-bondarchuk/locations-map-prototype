# locations-map-prototype
A one-file interactive locations map prototype built with HTML, CSS, JavaScript, Leaflet, and OpenStreetMap. Includes floating location list, live search, browser geolocation, nearest locations, marker popups, single-select sync, and shareable URL query params without page reload.
